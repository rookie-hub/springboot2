#spring:
#  mvc:
#    static-path-pattern: /res/**
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true # 开启页面表单的Rest功能

server:
  servlet:
    encoding:
      charset: utf-8
      force: true
      enabled: true
  tomcat:
    uri-encoding: utf-8